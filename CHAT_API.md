# 🤖 SmartWallet Chat API

## Описание

API для чата с AI ассистентом по финансовой грамотности. Ассистент использует GigaChat и имеет доступ к контексту пользователя (карты, транзакции).

## Эндпоинт

### POST /assistant/chat

Отправка сообщения ассистенту и получение ответа.

#### Запрос

**URL**: `POST /assistant/chat`

**Headers**:
```
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json
```

**Body**:
```json
{
  "message": "Привет, подскажи лучший кэшбэк на еду?"
}
```

#### Ответ

**Успешный ответ (200)**:
```json
{
  "reply": "Для категории 'Еда' выгоднее использовать Сбербанк Премьер (5%). У вас есть эта карта с номером ****1234."
}
```

**Ошибка (401)**:
```json
{
  "detail": "Could not validate credentials"
}
```

## Примеры использования

### 1. Базовый запрос

```bash
curl -X POST "http://localhost:8000/assistant/chat" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message": "Привет!"}'
```

### 2. Вопрос о кэшбэке

```bash
curl -X POST "http://localhost:8000/assistant/chat" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message": "Какая карта лучше для транспорта?"}'
```

### 3. Расчет кэшбэка

```bash
curl -X POST "http://localhost:8000/assistant/chat" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message": "Сколько кэшбэка я получу за покупку на 1000 рублей в категории еда?"}'
```

## Контекст пользователя

Ассистент имеет доступ к следующей информации о пользователе:

- **Профиль**: имя, телефон
- **Карты**: банк, название, последние 4 цифры, правила кэшбэка
- **Транзакции**: последние 5 транзакций с категориями и кэшбэком

## Возможности ассистента

- ✅ Рекомендации по выбору карты для категории
- ✅ Расчет кэшбэка для суммы
- ✅ Анализ трат пользователя
- ✅ Советы по оптимизации кэшбэка
- ✅ Ответы на вопросы по финансовой грамотности

## Тестирование

### Автоматический тест
```bash
python test_chat.py
```

### Интерактивный чат
```bash
python chat_client.py
```

### Тестовые данные
- **Телефон**: +79001234567
- **Пароль**: password123

## Обработка ошибок

- **ConnectionError**: Проблемы с интернет-соединением
- **Timeout**: Превышение времени ожидания ответа
- **HTTP ошибки**: Ошибки API GigaChat

## Ограничения

- Максимум 256 токенов в ответе
- Таймаут 30 секунд
- Требуется интернет-соединение для работы с GigaChat
